repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      # Vendored third-party files stay byte-identical to upstream: a
      # licence that has been reformatted is no longer quite the licence
      # that was granted, however cosmetic the edit.
      - id: trailing-whitespace
        exclude: ^src/sportsball/static/fonts/
      - id: end-of-file-fixer
        exclude: ^src/sportsball/static/fonts/
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: ty
        name: ty (type-check)
        entry: uv run ty check src
        language: system
        types: [python]
        pass_filenames: false

      - id: uv-export-requirements
        name: regenerate requirements.txt from uv.lock
        entry: uv export --no-dev --no-emit-project --quiet --output-file requirements.txt
        language: system
        files: '^(uv\.lock|pyproject\.toml)$'
        pass_filenames: false
